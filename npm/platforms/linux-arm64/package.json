{
  "name": "@jinba1/mthc-linux-arm64",
  "version": "0.2.0",
  "description": "Linux arm64 native binary for mthc.",
  "license": "MIT",
  "os": [
    "linux"
  ],
  "cpu": [
    "arm64"
  ],
  "scripts": {
    "prepack": "node -e \"const { accessSync, constants, statSync } = require('node:fs'); if (!statSync('bin/mthc').isFile()) throw new Error('bin/mthc must be a regular file'); accessSync('bin/mthc', constants.X_OK)\""
  },
  "files": [
    "bin/mthc"
  ]
}
